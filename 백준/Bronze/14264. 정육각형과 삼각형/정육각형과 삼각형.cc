#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false); cin.tie(NULL);
    double L;
    cout << fixed; cout.precision(13);
    cin >> L;
    cout << L * L * sqrt(3) / 4.0 << "\n";
    return 0;
}
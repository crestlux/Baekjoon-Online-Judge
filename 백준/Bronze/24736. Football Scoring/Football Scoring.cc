#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false); cin.tie(NULL);
    int a, b, c, d, e;
    for (int i = 0; i < 2; i++) {
        cin >> a >> b >> c >> d >> e;
        cout << 6 * a + 3 * b + 2 * c + d + 2 * e << " ";
    }
    return 0;
}
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false); cin.tie(NULL);
    cout << fixed; cout.precision(8);
    double p, q, r;
    cin >> p >> q >> r;
    cout << p * r / q << "\n";
    return 0;
}